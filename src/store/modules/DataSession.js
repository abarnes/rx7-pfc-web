import { SELECT_TIMESTAMP } from '@/store/actions'

const state = {
    currentDriveId: null,
    isLive: false
}

const mutations = {
    
  }
  
  const actions = {
    /*
    someAsyncTask ({ commit }) {
      // do something async
      commit('INCREMENT_MAIN_COUNTER')
    }
    */
  }
  
  export default {
    state,
    mutations,
    actions
  }